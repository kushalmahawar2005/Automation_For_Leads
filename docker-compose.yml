services:
  app:
    build: .
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production
      # HTTPS is now in front of the app (domain + SSL via nginx/certbot), so session
      # cookies are marked Secure. If you ever serve over plain HTTP again, set this
      # back to "false" or logins will loop (browsers drop Secure cookies over HTTP).
      COOKIE_SECURE: "true"
      DATABASE_URL: "${DATABASE_URL:-postgresql://neondb_owner:npg_iUeO7F8zXPSH@ep-hidden-thunder-avch87kx-pooler.c-11.us-east-1.aws.neon.tech/neondb?channel_binding=require&sslmode=require}"
      DATABASE_URL_UNPOOLED: "${DATABASE_URL_UNPOOLED:-postgresql://neondb_owner:npg_iUeO7F8zXPSH@ep-hidden-thunder-avch87kx.c-11.us-east-1.aws.neon.tech/neondb?sslmode=require}"
      # System Chromium installed in the image.
      PUPPETEER_EXECUTABLE_PATH: /usr/bin/chromium
      PUPPETEER_SKIP_DOWNLOAD: "true"
      # WhatsApp session is stored on the volume -> scan QR only ONCE.
      WWEBJS_AUTH_PATH: /data/wwebjs_auth
      # App config (edit these).
      ADMIN_EMAIL: "kushalmahawar71@gmail.com"
      SERP_API_KEY: ""
      # Email verification (Resend). Real values come from a .env file next to this
      # compose file on the VPS (gitignored, so the key never lands in git). If the
      # key is empty the OTP is logged to container logs instead of being emailed.
      RESEND_API_KEY: "${RESEND_API_KEY:-}"
      EMAIL_FROM: "${EMAIL_FROM:-Kushal Automation <onboarding@resend.dev>}"
    volumes:
      - wa-data:/data
    # Chromium needs a bigger /dev/shm or it crashes mid-linking.
    shm_size: "1gb"
    # Cap memory so a runaway Chromium can't kill the whole VPS.
    mem_limit: 2g

volumes:
  wa-data:
